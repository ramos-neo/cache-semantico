export const environment = {
  production: false,
  /**
   * Path relativo: o `ng serve` faz proxy de `/api` → `http://localhost:8000`.
   * Evita CORS em desenvolvimento.
   */
  apiBaseUrl: '/api',
};
